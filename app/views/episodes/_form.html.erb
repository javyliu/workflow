<div class="panel  callout radius">
  <h5><span>年度剩余假期</span></h5>
  <table>
    <tr>
      <% @user.check_types.each do |item| %>
      <th><%= item.third %></th>
      <% end %>
    </tr>
    <tr>
      <% @user.check_types.each do |item| %>
      <td><%=@user.calcute_remain_holiday(item)  %></td>
      <% end %>
    </tr>
  </table>
</div>
<%= form_for(@episode) do |f| %>
<div class="row">
  <div class="columns medium-4">
    <%= f.label :holiday_id,'类别' %>
    <%= f.hidden_field :holiday_id %>
    <input type="text" value="<%= @episode.holiday.try(:name) %>" readonly=true>

  </div>
  <div class="columns medium-4">
    <label for="">部门</label>
    <input type="text" value="<%= current_user.dept.name %>" readonly=true>
  </div>
  <div class="columns medium-4">
    <%= f.label :title,'职位' %>
    <%= f.select :title,Episode::Title,prompt: true %>
  </div>
</div>

<div class="row">
  <div class="columns medium-4">
    <%= f.label :start_date,"开始时间" %>
    <%= f.text_field :start_date,class: "datetimepicker",readonly: true ,required: true%>
  </div>
  <div class="columns medium-4 datetime">
    <%= f.label :end_date,"结束时间" %>
    <%= f.text_field :end_date,class: "datetimepicker",readonly: true ,required: true%>
  </div>
  <div class="columns medium-4">
    <div class="row collapse">
      <%= f.label :total_time,"申请时长" %>
      <div class="columns small-9">
        <%= f.text_field :total_time,required: true,pattern: "[.0-9]+" %>
      </div>
      <div class="columns small-3">
        <span class="postfix unit"><%= @episode.holiday.unit %></span>
      </div>
    </div>
  </div>
</div>
<div class="row">
  <div class="columns medium-12">
    <%= f.label :comment,"申请事由" %>
    <%= f.text_area :comment,required: true %>
  </div>
</div>
<div class="row">
  <div class="columns">
    <%= f.submit "提交",class: "button",data: {disable_with: "提交中,请稍候..."} %>
  </div>
</div>
<% end %>
<fieldset>
  <legend><span>说明</span></legend>
  <p>1、带薪病假及1天以上病假请附市级以上医院盖章的病假单。</p>
  <p>2、年假、特批、带薪事假、带薪病假的天数，请与人力资源部或部门确认后填写。</p>
  <p>3、特批/倒休最小请假单位1小时，其余假期最小请假单位0.5天。</p>
</fieldset>
